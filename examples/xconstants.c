#include <stdio.h>
#include <float.h>


int main()
{
  printf("DBL_EPSILON = %e\n", DBL_EPSILON);
  printf("DBL_MIN     = %e\n", DBL_MIN);
  printf("DBL_MAX     = %e\n", DBL_MAX);

  return 0;
}
